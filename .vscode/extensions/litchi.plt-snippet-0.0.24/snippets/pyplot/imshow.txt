fig, ax = plt.subplots(figsize=(5,4))
im = ax.imshow(array2d, cmap='coolwarm')

fig.colorbar(im, 
             ax=ax, 
             label='Interactive colorbar', 
             pad=0.05,
             shrink=1.00)
